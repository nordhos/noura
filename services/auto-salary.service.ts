import { Profile } from "@/services/profile.service";

export async function processAutoSalary(
  _profile: Profile,
  _today: Date
): Promise<void> {
  // Auto Salary dinonaktifkan sementara.
  // Akan diimplementasikan kembali pada Sprint 3
  // menggunakan arsitektur Income Source.
  return;
}